import pandas as pd

df=pd.read_csv('iris_mod.csv')
print(df.head(10))
print()
print(iris_csv.shape)

#from sklearn.linear_model import LinearRegression

condition = df['sepal_length'].isnull()
non_data = df.loc[condition]
print(non_data)

x = non_data.loc[:,'sepal_width':'petal_width']
pred = model.predict(x)
df.loc[condition, 'sepal_length'] = pred  
# Removed extra space in column name
print(pred)
